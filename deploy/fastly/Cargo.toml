[package]
name = "fairfetch-edge"
version = "0.1.0"
edition = "2021"
description = "Fairfetch edge compute for Fastly Compute@Edge"

[dependencies]
fastly = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
